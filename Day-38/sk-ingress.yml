apiVersion: v1
kind: Ingress
metadata:
  name: sk-ingress-resource
spec:
  rules:
  - host: "foo.bar.com"
    http:
      paths:
      - pathType: Prefix
        path: "/bar"
        backend:
          service:
            name: sk-service-resource
            port:
              number: 80
